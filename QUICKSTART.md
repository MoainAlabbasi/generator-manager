# دليل البدء السريع - Generator Manager

## التشغيل المحلي (للتطوير والاختبار)

### 1. استنساخ المستودع

```bash
git clone https://github.com/MoainAlabbasi/generator-manager.git
cd generator-manager
```

### 2. إنشاء بيئة افتراضية (اختياري لكن موصى به)

```bash
python3 -m venv venv
source venv/bin/activate  # على Linux/Mac
# أو
venv\Scripts\activate  # على Windows
```

### 3. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 4. تشغيل التطبيق

```bash
python main.py
```

أو باستخدام Flet:

```bash
flet run main.py
```

## بناء APK للأندرويد

### المتطلبات

- Python 3.10+
- Flutter SDK (يجب أن يكون مثبتاً ومضافاً إلى PATH)
- Android SDK

### خطوات البناء

```bash
# تأكد من تثبيت Flet
pip install flet

# بناء APK
flet build apk --verbose
```

سيتم إنشاء ملف APK في:
```
build/apk/app-release.apk
```

## البناء التلقائي عبر GitHub Actions

1. افتح المستودع على GitHub
2. اذهب إلى تبويب **Actions**
3. أضف workflow باستخدام المحتوى من ملف `WORKFLOW_SETUP.md`
4. بعد كل push إلى main، سيتم بناء APK تلقائياً
5. حمّل APK من صفحة Artifacts في workflow run

## الاستخدام الأساسي

### إضافة مولد جديد

1. اذهب إلى "التوريد والإعدادات"
2. اختر تبويب "الإعدادات"
3. أدخل اسم المولد واضغط "إضافة مولد"

### إضافة عداد جديد

1. في نفس الصفحة، اختر المولد من القائمة
2. أدخل نوع العداد (مثل: شاشة دمج)
3. أدخل القراءة الأولية
4. اضغط "إضافة عداد"

### تسجيل قراءة يومية

1. اذهب إلى "التسجيل اليومي"
2. اختر المولد
3. اختر العداد (سيتم جلب القراءة السابقة تلقائياً)
4. أدخل القراءة الحالية والديزل المستهلك
5. اضغط "حفظ السجل"

### تسجيل توريد ديزل

1. اذهب إلى "التوريد والإعدادات"
2. اختر تبويب "توريد الديزل"
3. أدخل التاريخ، الكمية، اسم المحطة، ورقم الإيصال
4. اضغط "حفظ التوريد"

### عرض التقارير

1. اذهب إلى "التقارير"
2. ستشاهد آخر 30 سجل مع تلوين تلقائي حسب الكفاءة:
   - **أخضر**: كفاءة > 3.2 كيلووات/لتر
   - **أحمر**: كفاءة < 3.0 كيلووات/لتر

### النسخ الاحتياطي

1. اذهب إلى "التوريد والإعدادات"
2. اختر تبويب "الإعدادات"
3. اضغط "تصدير قاعدة البيانات"
4. سيتم حفظ نسخة من قاعدة البيانات في مجلد Downloads

## الميزات الرئيسية

✅ **واجهة عربية كاملة** مع تصميم RTL
✅ **العمل بدون إنترنت** - قاعدة بيانات محلية
✅ **جلب تلقائي للقراءة السابقة** عند اختيار العداد
✅ **حسابات تلقائية** للإنتاج والكفاءة
✅ **تلوين تلقائي** للتقارير حسب الكفاءة
✅ **نسخ احتياطي سهل** لقاعدة البيانات
✅ **بناء APK تلقائي** عبر GitHub Actions

## المساعدة والدعم

للأسئلة والمشاكل، يرجى فتح Issue في المستودع:
https://github.com/MoainAlabbasi/generator-manager/issues

## الترخيص

مفتوح المصدر ومتاح للاستخدام الحر.
